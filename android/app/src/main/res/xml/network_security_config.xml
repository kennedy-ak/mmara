<?xml version="1.0" encoding="utf-8"?>
<!--
Network Security Configuration

- In DEBUG mode: allows cleartext traffic for local development
- In RELEASE mode: blocks all cleartext traffic, only HTTPS allowed

IMPORTANT: For production, ensure all API endpoints use HTTPS.
-->
<network-security-config>
    <!-- Debug configuration: allow cleartext for localhost testing -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartreat only for local development addresses -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
